import CityForm from 'src/pages/cities/Forms/CityForm'

const CityWindow = ({ labels, maxAccess, recordId }) => {
  return <CityForm labels={labels} maxAccess={maxAccess} recordId={recordId} />
}

export default CityWindow
